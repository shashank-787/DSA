    int remove_duplicate(int a[],int n){
        
        int j=0, i=0;
        for(i=0 ; i<n ; i++)
        {
            if(a[j] != a[i]){
                j++;
                a[j] = a[i];
            }
        }
        return j + 1;
        
    }
